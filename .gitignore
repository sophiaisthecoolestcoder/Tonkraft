# Claude Code agent folder — may contain credentials, auth tokens, or private artifacts
.claude/

# Source / working files that shouldn't ship with the site
*.mov
*.odt
Tonkraft Website Umsetzung.pdf
Vemu_1.jpg
vemusama2.jpg

# Transcoded films — kept local for now (479 MB; hosting TBD).
# Two files exceed GitHub's 100 MB/file limit. Force-add or use Git LFS
# when ready to ship them.
assets/videos/

# Source material handoffs (PDFs, TIFFs, audio masters) — only their
# web-ready derivatives in assets/ are shipped.
files/
